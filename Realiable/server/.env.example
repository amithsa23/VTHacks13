# Copy this file to server/.env (or project root .env) and fill in your keys. Do NOT commit real keys to git.
# The server will look for server/.env first, then the repository root .env.

# If you want to use Google PaLM (Gemini) as the LLM agent, set:
GEMINI_PROVIDER=google
GEMINI_API_KEY=YOUR_GEMINI_API_KEY
GEMINI_MODEL=text-bison-001

# If you want to use a generic LLM endpoint, set GEMINI_API_ENDPOINT in addition to GEMINI_API_KEY:
# GEMINI_API_ENDPOINT=https://your-llm-endpoint.example/v1/generate

# Note: Google Maps API key is no longer required. The server returns an SVG placeholder for /api/map/:id
# If you want to re-enable Google Static Maps proxy, set GOOGLE_MAPS_API_KEY here (not required for Gemini-only mode):
# GOOGLE_MAPS_API_KEY=YOUR_GOOGLE_MAPS_API_KEY
